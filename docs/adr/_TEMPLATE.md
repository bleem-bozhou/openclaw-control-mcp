# ADR-NNN — Title (one line)

**Status:** Accepted | Superseded by ADR-XXX | Deprecated
**Date:** YYYY-MM-DD

## Context

Why this decision was needed. What forces / constraints applied at the time.

## Decision

The chosen approach in 1-3 sentences. What we will do.

## Consequences

- Positive consequences (what gets easier).
- Negative consequences (what gets harder, what trade-off we accept).
- Files anchored: `src/path/file.ts` (mark with `// ADR-NNN` comment).

## Alternatives considered

Briefly: 1-2 paths not taken, with the reason.
